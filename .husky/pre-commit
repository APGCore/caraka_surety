# #!/bin/sh
# . "$(dirname "$0")/_/husky.sh"

# # Check for changes in .tsx files
# STAGED_FILES=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.tsx$')

# if [ -n "$STAGED_FILES" ]; then
#   echo "Changes detected in .tsx files. Running build..."
#   npm run build
# else
#   echo "No .tsx files staged. Skipping build."
# fi
npm run build
